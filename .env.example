# Frontend
VITE_API_BASE=http://localhost:8000
VITE_ENABLE_ADMIN_DEMO=false

# Django
DJANGO_DEBUG=1
DJANGO_SECRET_KEY=dev-secret-change-me
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database (SQLite by default; no config needed)
# For Postgres local opt-in, uncomment and adjust:
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=trailium
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_HOST=localhost
# DB_PORT=5432

# Demo data sources (override if needed)
DEMO_AVATAR_BASE=https://i.pravatar.cc
DEMO_PHOTO_BASE=https://picsum.photos

# Demo seed weights (override to tune demo composition)
DEMO_PREMIUM_RATE=0.15
DEMO_PRIVATE_RATE=0.10
